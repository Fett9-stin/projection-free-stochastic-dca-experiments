Note that:
Most of the plots use CPU times as one axis. Since the performance of
the CPU might vary differently each time, so the function value should be
reproducible for the same seeds, but the exact horizontal positions on the CPU-times axis
might slightly differ.


#--------SDCAFW instruction----------------------

To reproduce the experimental results from the thesis:

We can change the initialization for the whole algorithm
inbetween line 561-566 to switch from x_ini to 564-566 the randomized initial point

seed_data, seed_x0 (seed for the randomized initial point x_0), 
seed_for_5_algorithms can be modified in lines 680-682


AI declarations:
Plotting function and the auto-saving function are generated by ChatGPT 





#-------Matrix Completion with DC_SVRCGS instruction----

To reproduce the experimental results from the thesis:

Activate the time threshold of 2000s for "prox_dc_vr_scgs"
can be found in lines 496-499, comment out them or uncomment them
can switch the cases discussed in the thesis.

There is no seed for initialization x_0, since we pick the zero matrix in this experimental

The central seed that controls instance initialization, and all randomness for each algorithm
can be modified in line 798.


AI declarations:
Central seed bank, plotting function and the auto-saving function are generated by ChatGPT 





#-------Logistic Experiment with DC_SVRCGS instruction---------

Some comments about the algorithms can be found in Matrix completion project

To reproduce the experimental results from the thesis:

Switch the delta_t from 1/(t+1)^2(t+2)^2 to 1/(t+1)(t+2) for both "prox_dc_cr_scgs" and "dc_vr_scgs"
manually in lines 402-403 and 497-498.

The seed for initialization can be modified in line 639.

The central seed that controls the instance generating and randomness for all algorithms
can be modified in line 704.

The exact seed suggestions were detailed demonstrated 
under each plot in the thesis.

All other parameters that are not commented out are the parameters used in the experiments

AI declarations:
Central seed bank, plotting function and the auto-saving function are generated by ChatGPT 





